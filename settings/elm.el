;; Currently turn off elm-mode because there is some problem with project-root
;; function causing lsp-mode to fail.
;; See https://github.com/emacs-lsp/lsp-mode/issues/2335
;;
;; (use-package elm-mode
;;   :ensure t
;;   :config
;;   (setq elm-format-on-save t)
;;   )
